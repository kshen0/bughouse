// Generated by CoffeeScript 1.6.3
(function() {
  var createBoard, dark, light, squareSize;

  squareSize = 70;

  dark = "#34495e";

  light = "#95a5a6";

  oCanvas.domReady(function() {
    var canvas;
    canvas = oCanvas.create({
      canvas: "#board",
      background: dark
    });
    canvas.height = 8 * squareSize;
    canvas.width = 8 * squareSize;
    return createBoard(canvas);
    /*
    board = ((x for x in [1..8]) for x in [1..8])
    console.log board
    */

  });

  createBoard = function(canvas) {
    var createRectangle, x, y, _i, _results;
    createRectangle = function(x, y, color) {
      var rectangle;
      rectangle = canvas.display.rectangle({
        x: x,
        y: y,
        width: squareSize,
        height: squareSize,
        fill: color
      });
      return canvas.addChild(rectangle);
    };
    _results = [];
    for (x = _i = 0; _i <= 7; x = ++_i) {
      _results.push((function() {
        var _j, _results1;
        _results1 = [];
        for (y = _j = 0; _j <= 7; y = ++_j) {
          _results1.push(createRectangle(x * squareSize, y * squareSize, (y + x) % 2 === 0 ? light : dark));
        }
        return _results1;
      })());
    }
    return _results;
  };

}).call(this);
